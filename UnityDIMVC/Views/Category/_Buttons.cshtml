@model int

    <div class="btn-group">
        <a class="btn btn-primary btn-sm" href="@Url.Action("Edit","Category",new { ID=Model})">Edit</a>

        <a class="btn btn-success btn-sm" href="@Url.Action("Details","Category",new { ID=Model})">Details</a>

        <a class="btn btn-danger  btn-sm " href="@Url.Action("Delete","Category",new { ID=Model})">Delete</a>
    </div>
